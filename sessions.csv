session_id,subject,class_id,start,expiry,admin,device_lock,latitude,longitude
with open(SESSION_FILE) as f:
    for r in csv.reader(f):
        if r and r[5] == admin_id:
            if len(r) < 9:
                r.extend(["0", "0"])  # Default lat/lon if missing
            sessions.append(r)
